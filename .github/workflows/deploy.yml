name: Gatsby Publish

on:
   push:
      branches: [ master ]
   pull_request:
      branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: npm i && ./node_modules/.bin/gatsby build
