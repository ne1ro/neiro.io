name: Gatsby Publish

on:
  - pull_request
    # branches:
    #   - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: npm i && ./node_modules/.bin/gatsby build
